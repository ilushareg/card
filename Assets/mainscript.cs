using UnityEngine;
using System.Collections;

public class mainscript : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //UnityEngine.SceneManagement.SceneManager.LoadScene("a_UI", UnityEngine.SceneManagement.LoadSceneMode.Additive);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
